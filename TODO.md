# TODO

- [ ] Add AdSense script tag to `index.html` head
- [ ] Add at least one `<ins class="adsbygoogle">` ad slot where you want ads to appear
- [ ] Add `adsbygoogle.push({})` call after the ad slot
- [ ] Ensure we don’t duplicate scripts if `google2...html` already has verification only
- [ ] Test locally build + browser console for AdSense errors

